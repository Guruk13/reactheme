import { IGunGelMode, StyledNativeComponent, IStyledNativeComponent } from 'styled-components'

//dark/light mode
const GunGelMode: IGunGelMode = {
  borderRadius: '5px',

  colors: {
    main: 'cyan',
    secondary: 'magenta',
  },
}

const StyledNativeComponent: IStyledNativeComponent{
    

}